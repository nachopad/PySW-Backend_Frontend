import { ValidacionPrecioDirective } from './validacion-precio.directive';

describe('ValidacionPrecioDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionPrecioDirective();
    expect(directive).toBeTruthy();
  });
});
