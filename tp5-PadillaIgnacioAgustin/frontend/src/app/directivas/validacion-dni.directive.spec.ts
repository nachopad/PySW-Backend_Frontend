import { ValidacionDniDirective } from './validacion-dni.directive';

describe('ValidacionDniDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidacionDniDirective();
    expect(directive).toBeTruthy();
  });
});
