import { FormatoEspectadorPipe } from './formato-espectador.pipe';

describe('FormatoEspectadorPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoEspectadorPipe();
    expect(pipe).toBeTruthy();
  });
});
